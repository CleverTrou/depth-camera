# Depth Camera — Python dependencies
# All run on Raspberry Pi 5 (ARM64, no GPU)

flask>=3.1.3
pyyaml>=6.0.3
requests>=2.34.2

# Depth estimation
onnxruntime>=1.26.0          # Has ARM64 (aarch64) wheels on PyPI
numpy>=2.4.4
Pillow>=12.2.0

# Depth colormap visualization (optional — graceful fallback to grayscale)
matplotlib>=3.10.9
